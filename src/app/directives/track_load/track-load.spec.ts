import { TrackLoad } from './track-load';

describe('TrackLoad', () => {
  it('should create an instance', () => {
    const directive = new TrackLoad();
    expect(directive).toBeTruthy();
  });
});
